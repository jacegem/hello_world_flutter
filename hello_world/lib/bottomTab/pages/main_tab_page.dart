import 'package:flutter/material.dart';

class MainTabPage extends StatelessWidget {
  static const String routeName = '/maintab';
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Main Tab Page'),
    );
  }
}
