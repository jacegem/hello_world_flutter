import 'package:flutter/material.dart';

class EmailConfirmPage extends StatelessWidget {
  static const String routeName = '/emailconfirm';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Email Confirm Page'),
      ),
    );
  }
}
